CREATE OR REPLACE PACKAGE test_pkg_common
IS
    /*<>
       Unit testing package pkg_common.
    */

    /* =========================================================================
       Public Procedure Declaration.
       ---------------------------------------------------------------------- */

    --%suite
    --%rollback(manual)

    /* =========================================================================
       cutfirstitem.
       ---------------------------------------------------------------------- */

    --%test
    PROCEDURE cutfirstitem;

    /* =========================================================================
       duration.
       ---------------------------------------------------------------------- */

    --%test
    PROCEDURE duration;

    /* =========================================================================
       generateuniquekey.
       ---------------------------------------------------------------------- */

    --%test
    PROCEDURE generateuniquekey;

    /* =========================================================================
       getdatesforperiod.
       ---------------------------------------------------------------------- */

    --%test
    PROCEDURE getdatesforperiod;

    /* =========================================================================
       getharderrordesc.
       ---------------------------------------------------------------------- */

    --%test
    --%aftertest(getharderrordesc_after)
    PROCEDURE getharderrordesc;

    PROCEDURE getharderrordesc_after;

    /* =========================================================================
       getrequiredheaderfield.
       ---------------------------------------------------------------------- */

    --%test
    PROCEDURE getrequiredheaderfield;

    --%test
    --%throws(PKG_COMMON.excp_missing_header_fld)
    PROCEDURE getrequiredheaderfield_1005_1;

    --%test
    --%throws(PKG_COMMON.excp_missing_header_fld)
    PROCEDURE getrequiredheaderfield_1005_2;

    --%test
    --%throws(PKG_COMMON.excp_missing_header_fld)
    PROCEDURE getrequiredheaderfield_1005_3;

    --%test
    --%throws(PKG_COMMON.excp_missing_header_fld)
    PROCEDURE getrequiredheaderfield_1005_4;

    /* =========================================================================
       insert_warning.
       ---------------------------------------------------------------------- */

    --%test
    --%aftertest(insert_warning_after)
    PROCEDURE insert_warning;

    PROCEDURE insert_warning_after;

    /* =========================================================================
       is_alphanumeric.
       ---------------------------------------------------------------------- */

    --%test
    PROCEDURE is_alphanumeric;

    /* =========================================================================
       is_integer.
       ---------------------------------------------------------------------- */

    --%test
    PROCEDURE is_integer;

    /* =========================================================================
       is_numeric.
       ---------------------------------------------------------------------- */

    --%test
    PROCEDURE is_numeric;

    /* =========================================================================
       isdoneinperiod.
       ---------------------------------------------------------------------- */

    --%test
    PROCEDURE isdoneinperiod;

    /* =========================================================================
       istimeforprocess.
       ---------------------------------------------------------------------- */

    --%test
    PROCEDURE istimeforprocess;

    /* =========================================================================
       l.
       ---------------------------------------------------------------------- */

    --%test
    --%aftertest(l_after)
    PROCEDURE l;

    PROCEDURE l_after;

    /* =========================================================================
       lb.
       ---------------------------------------------------------------------- */

    --%test
    --%aftertest(lb_after)
    PROCEDURE lb;

    PROCEDURE lb_after;

    /* =========================================================================
       sp_db_sleep.
       ---------------------------------------------------------------------- */

    --%test
    PROCEDURE sp_db_sleep;

    /* =========================================================================
       sp_insert_warning.
       ---------------------------------------------------------------------- */

    --%test
    --%aftertest(sp_insert_warning_after)
    PROCEDURE sp_insert_warning;

    PROCEDURE sp_insert_warning_after;

    /* =========================================================================
       sp_is_numeric.
       ---------------------------------------------------------------------- */

    --%test
    PROCEDURE sp_is_numeric;

    /* =========================================================================
       speed.
       ---------------------------------------------------------------------- */

    --%test
    PROCEDURE speed;
END test_pkg_common;
/